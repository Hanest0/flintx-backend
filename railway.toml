[build]
builder = "nixpacks"

[build.nixpacks]
ignoreBuildSecrets = true

[deploy]
startCommand = "uvicorn flint.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
